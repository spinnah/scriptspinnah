import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings go here")
                .font(.title)
                .padding()

            Text("This will be your script + folder pairing UI.")
        }
        .frame(width: 400, height: 300)
        .padding()
    }
}